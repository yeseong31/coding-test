def solution(name):
    n = len(name)
    alpha = ['A'] * n
    left_cnt, right_cnt = 0, 0

    # 한 글자씩 이동하면서 세팅
    for i in range(n):
        pass

    return min(left_cnt, right_cnt)


name = "JEROEN"
print(solution(name))

name = "JAN"
print(solution(name))
